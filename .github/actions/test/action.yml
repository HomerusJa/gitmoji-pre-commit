name: Tests
description: Run tests on the code.

runs:
  using: 'composite'
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@v3.2.2
      with:
        enable-cache: true

    - name: Run pytest
      run: uv run pytest
      shell: bash
